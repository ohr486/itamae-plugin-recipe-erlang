box: ruby

default: &default
  after-steps:
    - script:
      name: set vars
      code: |
        export RBENV_VERSION=2.3.1
    - script:
      name: remove all vms
      code: vagrant destroy -f

build:
  steps:

build-centos7:
  <<: *default
  steps:
    - script:
      name: setup
      code: ./ci/setup.sh
    - bundle-install:
      jobs: 4
    - script:
      name: build CentOS 7.0
      code: ./ci/build.sh centos7
